export enum DataBaseTableNames {
  USER = 'users',
  POST = 'posts',
  CATEGOTY = 'categories',
  TOP_POST = 'top_posts',
}

export enum DataBaseModelNames {
  USER = 'user',
  POST = 'post',
  CATEGOTY = 'category',
  TOP_POST = 'top_post',
}
