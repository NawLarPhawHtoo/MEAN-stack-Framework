export enum DataBaseTableNames {
  USER = 'users',
  POST = 'posts',
  CATEGOTY = 'categories',
  TOP_POST = 'top_posts',
  PASSWORD_RESET = 'password_reset'
}

export enum DataBaseModelNames {
  USER = 'user',
  POST = 'post',
  CATEGOTY = 'category',
  TOP_POST = 'top_post',
  PASSWORD_RESET = 'password_reset'
}
